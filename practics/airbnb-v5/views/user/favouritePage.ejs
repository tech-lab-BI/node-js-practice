<%- include('../partials/headHtml')%>
<body class="bg-gray-50">
    <%- include('../partials/header')%>
    <main class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">My Favourite Homes</h1>
        <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% homes.forEach((home) => {%>
                <%- include('../partials/homeList', {home: home})%>
                    <form action="/user/deleteFav/<%=home.id%>" method="POST">
                        <button class="w-full bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-yellow-600 transition">Remove from favourite</button>
                    </form>
                    <a href="#" class="block mt-6">
                        <button class="w-full bg-red-500 text-white py-3 rounded-lg font-semibold hover:bg-red-600 transition">Book Now</button>
                    </a>
                    </div>
                </li>
            <%});%>
        </ul>
    </main>
    <%- include('../partials/footer')%>
</body>
</html>